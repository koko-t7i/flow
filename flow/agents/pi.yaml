interface:
  display_name: "Flow"
  short_description: "Adaptive workspace flow for repository changes"
  default_prompt: "Use /flow to choose a safe workspace, implement and verify repository changes, and clean merged task resources; create optional worktrees under .worktrees/<repo>-<branch>."
