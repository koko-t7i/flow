interface:
  display_name: "Flow"
  short_description: "Adaptive workspace flow for repository changes"
  default_prompt: "Use $flow to choose a safe checkout or optional in-repo worktree, implement and verify changes, then clean merged task resources."
