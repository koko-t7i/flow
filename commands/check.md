---
description: Read-only localflow environment capability snapshot
argument-hint: ""
---

# Localflow check

Use the `localflow:localflow` skill for this request. If the Skill tool is available, invoke `localflow:localflow` before continuing, then follow that skill's workflow exactly.

This command always runs the **`check`** subcommand: follow the skill's `### localflow check` section exactly. It is a read-only environment capability snapshot — do not clarify requirements, create branches, edit repository files, commit, push, or clean worktrees.

Additional arguments (if any):

```text
$ARGUMENTS
```
