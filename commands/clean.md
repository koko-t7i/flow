---
description: Clean only already-landed branches/worktrees/remotes
argument-hint: ""
---

# Localflow clean

Use the `localflow:localflow` skill for this request. If the Skill tool is available, invoke `localflow:localflow` before continuing, then follow that skill's workflow exactly.

This command always runs the **`clean`** subcommand: follow the skill's `### localflow clean` section exactly. It cleans only already-landed branches, worktrees, and remotes — it does not touch unlanded work.

Additional arguments (if any):

```text
$ARGUMENTS
```
