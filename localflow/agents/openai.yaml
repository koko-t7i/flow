interface:
  display_name: "Localflow"
  short_description: "本地仓库 flow skill"
  default_prompt: "Use $localflow as a repository flow skill: understand, orient, assign the right agent when useful, keep normal work on the environment branch, isolate only when needed, implement, verify, commit task-owned paths, deliver, and clean up only safe landed work. Use minimal repo checks. Stage task-owned paths only. Ask before destructive git actions."
